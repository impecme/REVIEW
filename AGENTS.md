# 复习课程整理规则

本工作区用于课程复习资料整理。所有 Agent 在本目录及其子目录中工作时，应遵守以下规则。

## 总原则

- 课程整理内容必须严格参照用户指定的 PPT、PDF、课堂笔记、图片、作业题或真题。
- 不要凭空扩展课程外知识；如果资料没有明确提供，必须标注“PPT 中未明确提供”或“资料中未明确提供”。
- 未经用户明确要求，不要修改具体课程整理文件。
- 修改前必须明确本次要改的文件和范围。
- 保留已有图片引用，并在需要时补充图片说明、图中重点和可能考法。

## 复习文档固定结构

课程复习文档必须采用以下结构：

```markdown
# 目录

# 要点速背

## 高频要点 1

## 高频公式 / 关键结论 1

## 易混淆点 1

# 第 1 章 具体章节标题

## 该章内的知识点 1

## 该章内的知识点 2

# 第 2 章 具体章节标题

## 该章内的知识点 1

## 该章内的知识点 2

# 作业题

## 题 1

## 题 2
```

## 标题规则

- `#` 一级标题只能用于：`目录`、`要点速背`、具体章节、`作业题`。
- 课程正文的一级标题必须是具体章节，例如 `# 第 1 章 硅和硅片的制备`。
- `# 目录` 必须放在文档最开头。
- `# 要点速背` 必须紧跟 `# 目录` 之后；其中每一个速背要点必须单独作为一个 `##` 二级标题。
- 各章一级标题下，`##` 二级标题必须按该章实际知识点细分，不能直接写成“核心概念”“重要公式”“考试重点”等通用分类。
- `# 作业题` 必须放在文档最后；其中每一个题必须单独作为一个 `##` 二级标题。
- 复习文档不使用三级标题及更低级标题，即不得使用 `###`、`####` 等标题。

## 内容整理规则

- `# 目录` 必须放在文档开头，用于列出 `# 要点速背`、各章标题和 `# 作业题`。
- `# 要点速背` 必须紧跟目录之后。
- `# 要点速背` 只放全课程高频考点、高频公式、关键结论、易混淆点，不堆长篇解释。
- `# 要点速背` 下的每个 `##` 二级标题就是一个可直接背诵的要点，标题后可用短表格、短列表或简短说明展开。
- 每一章都必须进行汇总复习。
- 每章内部按该章实际知识点拆分为 `##` 二级标题。
- 定义、公式、流程、优缺点、适用场景必须归入对应章节的具体知识点。
- 长段文字优先整理成表格、步骤列表或 Mermaid 图。
- 作业题、真题、课后题统一放在文档末尾的 `# 作业题` 中，每一道题单独作为一个 `##` 二级标题；需要章节归属时写入题目标题或题目内容中。

## NotebookLM 配合规则

- NotebookLM 用于上传多源资料、来源溯源、学习指南、FAQ、Mind Map 和遗漏复核。
- 不使用 NotebookLM 的 Audio Overview / 音频摘要作为主要复习方式。
- 如果通过 MCP 实现 Agent 和 NotebookLM 联动，只需要维护一份统一提示词；Agent 应按统一提示词调度资料读取、NotebookLM 复核和本地文件更新，但复习文档格式必须以本 `AGENTS.md` 为最高优先级。
- NotebookLM 复核结果应按 `PPT 章节 -> 知识点 -> 遗漏内容 -> 来源资料 -> 建议补充位置` 输出。
- 将 NotebookLM 的遗漏清单交给 Agent 更新本地复习文档时，Agent 只能按遗漏清单补充，不主动扩展。

## 问答规则

- 后续回答用户课程问题时，优先基于整理后的本地复习文档。
- 回答应先给直接答案，再指出依据来自哪个章节、哪个知识点。
- 如果整理内容中没有依据，应明确说“整理内容中未提供”。
- 如果用户问考试题，应给出适合卷面作答的标准表述。
