# 复习课程整理规则

本工作区用于课程复习资料整理。所有 Agent 在本目录及其子目录中工作时，应遵守以下规则。

## 总原则

- 课程整理内容必须严格参照用户指定的 PPT、PDF、课堂笔记、图片、作业题或真题。
- 不要凭空扩展课程外知识；如果资料没有明确提供，必须标注“PPT 中未明确提供”或“资料中未明确提供”。
- 未经用户明确要求，不要修改具体课程整理文件。
- 修改前必须明确本次要改的文件和范围。
- 保留已有图片引用，并在需要时补充图片说明、图中重点和可能考法。

## 复习文档固定结构

课程复习文档必须采用以下结构：

```markdown
# 目录

# 速背

# 第 1 章 具体章节标题

## 该章内的大知识点

### 核心概念

### 关键结论 / 公式

### 流程 / 推导 / 方法

### 易错点

### 可能考法

# 第 2 章 具体章节标题

## 该章内的大知识点

### 核心概念

### 关键结论 / 公式

### 流程 / 推导 / 方法

### 易错点

### 可能考法

# 作业题
```

## 标题规则

- `#` 一级标题只能用于：`目录`、`速背`、具体章节、`作业题`。
- 课程正文的一级标题必须是具体章节，例如 `# 第 1 章 硅和硅片的制备`。
- `##` 二级标题必须是该章节内的大知识点，不能直接写成“核心概念”“重要公式”“考试重点”等通用分类。
- `###` 三级标题用于大知识点内部细分，例如核心概念、公式、流程、易错点、可能考法。
- `# 作业题` 必须放在文档最后，并按章节归类整理。

## 内容整理规则

- `# 目录` 必须放在文档开头。
- `# 速背` 必须紧跟目录之后。
- `# 速背` 只放全课程高频考点、高频公式、关键结论、易混淆点，不堆长篇解释。
- 每一章都必须进行汇总复习。
- 每章内部按该章实际大知识点拆分。
- 定义、公式、流程、优缺点、适用场景必须归入对应章节的大知识点。
- 长段文字优先整理成表格、步骤列表或 Mermaid 图。
- 作业题、真题、课后题统一放在文档末尾的 `# 作业题` 中，并按章节归类。

## NotebookLM 配合规则

- NotebookLM 用于上传多源资料、来源溯源、学习指南、FAQ、Mind Map 和遗漏复核。
- 不使用 NotebookLM 的 Audio Overview / 音频摘要作为主要复习方式。
- NotebookLM 复核结果应按 `PPT 章节 -> 大知识点 -> 遗漏内容 -> 来源资料 -> 建议补充位置` 输出。
- 将 NotebookLM 的遗漏清单交给 Agent 更新本地复习文档时，Agent 只能按遗漏清单补充，不主动扩展。

## 问答规则

- 后续回答用户课程问题时，优先基于整理后的本地复习文档。
- 回答应先给直接答案，再指出依据来自哪个章节、哪个大知识点。
- 如果整理内容中没有依据，应明确说“整理内容中未提供”。
- 如果用户问考试题，应给出适合卷面作答的标准表述。

